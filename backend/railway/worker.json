{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": ["/backend/**"]
  },
  "deploy": {
    "startCommand": "python -m app.modules.orders.worker",
    "restartPolicyType": "ALWAYS"
  }
}
